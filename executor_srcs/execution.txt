current execution status: 


1.builtins : all builtins are working correctly except:export,cd (when no args are passed)

2.simple Commands: simple commands without redirections work correctly,(error handling is mostly solid here)

3. redirections :mostly work, but:some error handling cases still need to be reviewed(error messages),
heredoc has a few edge cases are not handled yet,
redirections with no (*target) need to be rechecked
,some cases where are used with no cmd args behave incorrectly 

4.pipes work correctly in general.
but pipes combined with redirections are not , and multiple pipes need to be rechecked





afsjoiejaiofjsidoajfiodasj


